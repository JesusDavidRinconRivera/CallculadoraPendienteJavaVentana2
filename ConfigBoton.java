package CalcPendiente;
import javax.swing.*;

public class ConfigBoton {
    public static JButton crearBoton(String etiqueta) {
        JButton boton = new JButton(etiqueta);
        return boton;
    }
    
}
